name: Deploy to AWS

on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

env:
  AWS_REGION: us-east-1
  ECR_SERVER_REPOSITORY: flyingforge-server
  ECS_CLUSTER: flyingforge-cluster
  ECS_SERVER_SERVICE: flyingforge-server

jobs:
  # Run tests first
  test-server:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: flyingforge_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: server/go.sum
      
      - name: Run tests
        env:
          DATABASE_URL: postgres://test:test@localhost:5432/flyingforge_test?sslmode=disable
          REDIS_URL: redis://localhost:6379
        run: go test ./... -v

  test-web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Type check
        run: npx tsc --noEmit
      
      - name: Lint
        run: npm run lint
      
      - name: Run tests
        run: npm test -- --passWithNoTests

  preflight-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum

      - name: Preflight deploy auth secrets
        env:
          AUTH_JWT_SECRET: ${{ secrets.AUTH_JWT_SECRET }}
          MCP_AUTH_PRIVATE_KEY_PEM: ${{ secrets.MCP_AUTH_PRIVATE_KEY_PEM }}
        run: go run ./cmd/deploy-preflight

  # Build and push Docker images
  build-and-push:
    needs: [test-server, test-web, preflight-deploy]
    runs-on: ubuntu-latest
    outputs:
      server_image: ${{ steps.build-server.outputs.image }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push server image
        id: build-server
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build --platform linux/amd64 -t $ECR_REGISTRY/$ECR_SERVER_REPOSITORY:$IMAGE_TAG -t $ECR_REGISTRY/$ECR_SERVER_REPOSITORY:latest -f Dockerfile.server .
          docker push $ECR_REGISTRY/$ECR_SERVER_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_SERVER_REPOSITORY:latest
          echo "image=$ECR_REGISTRY/$ECR_SERVER_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

  # Deploy infrastructure with Terraform
  terraform:
    needs: build-and-push
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    outputs:
      web_bucket_name: ${{ steps.tf-outputs.outputs.web_bucket_name }}
      cloudfront_distribution_id: ${{ steps.tf-outputs.outputs.cloudfront_distribution_id }}
      web_url: ${{ steps.tf-outputs.outputs.web_url }}
      mcp_url: ${{ steps.tf-outputs.outputs.mcp_url }}
      mcp_protected_resource_metadata_url: ${{ steps.tf-outputs.outputs.mcp_protected_resource_metadata_url }}
      mcp_openid_configuration_url: ${{ steps.tf-outputs.outputs.mcp_openid_configuration_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.0

      - name: Terraform Init
        run: terraform init

      # One-time migration safety: ensure the ALB listener detaches from the legacy
      # web target group before Terraform tries to destroy it. Without this, AWS
      # can return ResourceInUse if the destroy runs before the listener update.
      - name: Pre-apply ALB listener updates
        env:
          TF_VAR_auth_jwt_secret: ${{ secrets.AUTH_JWT_SECRET }}
          TF_VAR_mcp_auth_private_key_pem: ${{ secrets.MCP_AUTH_PRIVATE_KEY_PEM }}
        run: |
          terraform apply -auto-approve \
            -var="google_client_id=${{ secrets.GOOGLE_CLIENT_ID }}" \
            -var="google_client_secret=${{ secrets.GOOGLE_CLIENT_SECRET }}" \
            -var="encryption_key=${{ secrets.ENCRYPTION_KEY }}" \
            -var="mcp_auth_self_hosted=true" \
            -var="mcp_auth_key_id=ff-self-hosted" \
            -var="domain_name=flyingforge.com" \
            -var="create_dns_record=true" \
            -var="enable_vpc_endpoints=false" \
            -var="enable_nat_gateway=false" \
            -target="aws_lb_listener.http" \
            -target="aws_lb_listener.https[0]" \
            -target="aws_lb_listener_rule.api_https[0]"

      - name: Terraform Plan
        env:
          TF_VAR_auth_jwt_secret: ${{ secrets.AUTH_JWT_SECRET }}
          TF_VAR_mcp_auth_private_key_pem: ${{ secrets.MCP_AUTH_PRIVATE_KEY_PEM }}
        run: |
          # Cost optimization: disable NAT gateways and VPC endpoints; tasks egress via the internet gateway.
          terraform plan \
            -var="google_client_id=${{ secrets.GOOGLE_CLIENT_ID }}" \
            -var="google_client_secret=${{ secrets.GOOGLE_CLIENT_SECRET }}" \
            -var="encryption_key=${{ secrets.ENCRYPTION_KEY }}" \
            -var="mcp_auth_self_hosted=true" \
            -var="mcp_auth_key_id=ff-self-hosted" \
            -var="domain_name=flyingforge.com" \
            -var="create_dns_record=true" \
            -var="enable_vpc_endpoints=false" \
            -var="enable_nat_gateway=false" \
            -out=tfplan

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan

      - name: Export Terraform outputs
        id: tf-outputs
        run: |
          echo "web_bucket_name=$(terraform output -raw web_bucket_name)" >> "$GITHUB_OUTPUT"
          echo "cloudfront_distribution_id=$(terraform output -raw cloudfront_distribution_id)" >> "$GITHUB_OUTPUT"
          echo "web_url=$(terraform output -raw web_url)" >> "$GITHUB_OUTPUT"
          echo "mcp_url=$(terraform output -raw mcp_url)" >> "$GITHUB_OUTPUT"
          echo "mcp_protected_resource_metadata_url=$(terraform output -raw mcp_protected_resource_metadata_url)" >> "$GITHUB_OUTPUT"
          echo "mcp_openid_configuration_url=$(terraform output -raw mcp_openid_configuration_url)" >> "$GITHUB_OUTPUT"

  # Deploy to ECS
  deploy-server:
    needs: [build-and-push, terraform]
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Force new deployment for server
        run: |
          aws ecs update-service \
            --cluster $ECS_CLUSTER \
            --service $ECS_SERVER_SERVICE \
            --force-new-deployment

      - name: Wait for ECS services stability
        run: |
          set -euo pipefail

          wait_for_service_stability() {
            local service_name="$1"

            # The AWS CLI waiter has a fixed max wait time (~10 minutes) and can time out
            # during slower deployments (e.g., ALB deregistration delay). Retry to extend
            # the effective wait time while still using the built-in stable condition.
            local max_attempts=3
            for attempt in $(seq 1 "$max_attempts"); do
              echo "::group::Wait for ${service_name} stability (attempt ${attempt}/${max_attempts})"
              set +e
              aws ecs wait services-stable \
                --cluster "$ECS_CLUSTER" \
                --services "${service_name}"
              local rc=$?
              set -e
              echo "::endgroup::"

              if [ "$rc" -eq 0 ]; then
                echo "✅ ${service_name} has reached a steady state."
                aws ecs describe-services \
                  --cluster "$ECS_CLUSTER" \
                  --services "${service_name}" \
                  --query 'services[0].events[0:5].[createdAt,message]' \
                  --output table || true
                return 0
              fi

              echo "Waiter failed (exit code $rc). Dumping recent service events for debugging:"
              aws ecs describe-services \
                --cluster "$ECS_CLUSTER" \
                --services "${service_name}" \
                --query 'services[0].deployments[*].{status:status,rollout:rolloutState,desired:desiredCount,running:runningCount,pending:pendingCount,updatedAt:updatedAt,taskDef:taskDefinition}' \
                --output table || true
              aws ecs describe-services \
                --cluster "$ECS_CLUSTER" \
                --services "${service_name}" \
                --query 'services[0].events[0:10].[createdAt,message]' \
                --output table || true

              if [ "$attempt" -lt "$max_attempts" ]; then
                echo "Retrying in 15 seconds..."
                sleep 15
              fi
            done

            echo "❌ ${service_name} did not become stable in time."
            return 1
          }

          wait_for_service_stability "$ECS_SERVER_SERVICE"

      - name: Print URLs
        run: |
          echo "🚀 Server deployed successfully!"
          echo "📍 Web: ${{ needs.terraform.outputs.web_url }}"
          echo "📍 API: https://api.flyingforge.com"
          echo "📍 MCP: ${{ needs.terraform.outputs.mcp_url }}"
          echo "📍 MCP protected resource metadata: ${{ needs.terraform.outputs.mcp_protected_resource_metadata_url }}"
          echo "📍 MCP OpenID configuration: ${{ needs.terraform.outputs.mcp_openid_configuration_url }}"

  deploy-web:
    needs: terraform
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build web
        env:
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          VITE_GA_MEASUREMENT_ID: ${{ secrets.GA_MEASUREMENT_ID }}
          # NOTE: VITE_API_URL is the origin (no "/api" suffix). Frontend code appends "/api" internally.
          VITE_API_URL: ${{ github.event.inputs.environment == 'staging' && 'https://staging.flyingforge.com' || 'https://flyingforge.com' }}
          # Keep relative API clients same-origin (CloudFront forwards "/api*" to the ALB).
          VITE_API_BASE_URL: ''
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync to S3
        env:
          WEB_BUCKET: ${{ needs.terraform.outputs.web_bucket_name }}
        run: |
          aws s3 sync dist "s3://${WEB_BUCKET}" --delete

      - name: Invalidate CloudFront
        env:
          DIST_ID: ${{ needs.terraform.outputs.cloudfront_distribution_id }}
        run: |
          INVALIDATION_ID=$(aws cloudfront create-invalidation --distribution-id "$DIST_ID" --paths "/*" --query 'Invalidation.Id' --output text)
          aws cloudfront wait invalidation-completed --distribution-id "$DIST_ID" --id "$INVALIDATION_ID"
